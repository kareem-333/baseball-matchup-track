# Preflight Screenshots

These screenshots capture the app state before the refactor/data-layers refactor.

To capture:
  streamlit run dashboard.py
  Then screenshot: game selector, MASH scores, MISS scores, lineup view, day picker.

Use these as the regression baseline when verifying Phase F output.
